import React from 'react';
import ReactDOM from 'react-dom/client';
import SpaceTest from './SpaceTest';

const root = ReactDOM.createRoot(
  document.getElementById('root') as HTMLElement
);
root.render(<SpaceTest />);

